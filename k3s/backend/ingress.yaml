apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend
  namespace: monospaced
spec:
  rules:
    - host: api.rohitpotato.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 80
